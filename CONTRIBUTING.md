# 🔧 개발 가이드

## 브랜치 전략 (Git Flow)

### 브랜치 구조

```
main (배포용, 보호됨)
  └── develop (개발 통합)
        └── feature/* (새 기능)
```

### 브랜치 설명

- **main**: 프로덕션 배포 브랜치
  - GitHub Pages로 배포되는 안정 버전
  - 직접 커밋 금지
  - PR을 통해서만 병합

- **develop**: 개발 통합 브랜치
  - 다음 릴리스 준비
  - feature 브랜치들이 여기로 병합됨
  - 테스트 완료 후 main으로 PR

- **feature/***: 기능 개발 브랜치
  - 새 기능마다 생성
  - develop에서 분기
  - 완료 후 develop으로 PR

## 개발 워크플로우

### 1. 새 기능 개발 시작

```bash
# develop 브랜치에서 최신 코드 받기
git checkout develop
git pull origin develop

# 새 feature 브랜치 생성
git checkout -b feature/기능이름
```

### 2. 개발 및 커밋

```bash
# 작업 후 커밋
git add .
git commit -m "feat: 기능 설명"

# 원격에 푸시
git push origin feature/기능이름
```

### 3. Pull Request 생성

- GitHub에서 PR 생성
- `feature/기능이름` → `develop`
- 리뷰 후 병합

### 4. develop → main 배포

- develop에서 충분히 테스트 완료
- **성민님 승인 필요**
- PR 생성: `develop` → `main`
- 승인 후 병합 → 자동 배포

## 커밋 메시지 규칙

```
feat: 새 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 스타일 변경 (기능 변경 없음)
refactor: 리팩토링
test: 테스트 추가
chore: 빌드/설정 변경
```

### 예시

```bash
git commit -m "feat: 카카오톡 공유 기능 추가"
git commit -m "fix: 결과 페이지 모바일 레이아웃 수정"
git commit -m "docs: README에 사용법 추가"
```

## 주요 규칙

✅ **항상 하기:**
- feature 브랜치에서 개발
- 의미 있는 커밋 메시지
- develop으로 PR 생성
- main 병합은 성민님 승인 필요

❌ **하지 않기:**
- main 브랜치에 직접 커밋
- develop에 직접 푸시 (PR 사용)
- 큰 기능을 한 커밋으로

## 예시 시나리오

### 새 기능: 카카오톡 공유

```bash
# 1. feature 브랜치 생성
git checkout develop
git pull origin develop
git checkout -b feature/kakao-share

# 2. 개발
# ... 코드 작성 ...

# 3. 커밋 & 푸시
git add .
git commit -m "feat: 카카오톡 공유 기능 추가"
git push origin feature/kakao-share

# 4. GitHub에서 PR 생성
# feature/kakao-share → develop

# 5. 리뷰 & 병합 후
git checkout develop
git pull origin develop
git branch -d feature/kakao-share
```

---

**자동화 도구:** AI 어시스턴트가 이 규칙을 따라 자동으로 브랜치 생성 및 PR 생성을 도와드립니다!
